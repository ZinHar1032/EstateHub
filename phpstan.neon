includes:
    - phpstan-baseline.neon

parameters:
    level: 5
    paths:
        - app
        - routes
        - tests
    tmpDir: build/phpstan

    ignoreErrors: []

    excludePaths:
        - .git
        - bootstrap/cache
        - node_modules
        - vendor
        - storage
        - public
